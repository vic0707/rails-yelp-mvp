<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.error_notification %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.button :submit, "Send new review", class: "btn btn-danger" %>
<% end %>